# Local practice only. Replace the password and signing key before running.
DB_USER=example_user
DB_PASS=example_local_password_change_me
DB_NAME=example_db
JWT_SECRET_KEY=example_local_signing_key_replace_with_random_value
